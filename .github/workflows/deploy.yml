# ZAD deploy (#729) — skelet. workflow_dispatch zodat het niet auto-draait op de lege repo.
name: deploy-fsc-testnet
on:
  workflow_dispatch:

# Least-privilege: deze placeholder heeft geen token-rechten nodig. TODO(#729):
# verhoog per job naar het minimum dat zad-actions vereist.
permissions: {}

jobs:
  # TODO(#729): per peer + directory een job (geïsoleerde ZAD-projecten), parallel,
  # SHA-pinned zad-actions, conform het FBS-deploy.yml-patroon.
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO(#729) zad-actions deploy directory en peers"
