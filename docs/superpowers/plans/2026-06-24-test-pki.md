# Test-PKI (#722) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Een test-PKI onder `pki/` die OpenFSC's cfssl-tooling spiegelt: groep-trust-anchor (root→intermediate) + per-peer external endpoint-certs (`serialNumber`=OIN, `O`=peer-naam) + lege CRL.

**Architecture:** cfssl genereert root- en intermediate-CA en per-endpoint leaf-certs uit committe `csr.json`-templates. Scripts draaien lokaal bij de operator; sleutels/certs gaan naar gitignored `pki/ca/` en `pki/out/` — nooit in de repo, CI of image. Spiegelt `open-fsc/pki/` (`init.sh`/`issue.sh`/`fix-permissions.sh`) + `open-fsc/ca/config.json`.

**Tech Stack:** bash, cfssl + cfssljson (CloudFlare PKI toolkit), openssl (verificatie).

Spec: `docs/superpowers/specs/2026-06-24-test-pki-design.md`.

> **Update (na FBS-OIN-inventarisatie):** `magazijn-org` is later gesplitst in `magazijn-a`/`magazijn-b`
> met echte OIN's; `uitvraag-org`/`directory` kregen synthetische test-OIN's. Actuele toewijzing in
> `pki/README.md` → "Peers & OIN's". De `magazijn-org`/dummy-OIN's hieronder zijn het oorspronkelijke skelet.

## Global Constraints

- **Secrets nooit committen.** Alleen scripts, `config.json`/`*.json`-CA-configs en `csr.json`-templates in git. Keys/certs/CRL → gitignored.
- **cfssl draait alleen lokaal** bij de operator. Geen cert-generatie in CI of in een container-image.
- `subject.serialNumber` = OIN (wordt Peer ID); `subject.organization` (`O`) = peer-naam; `subject.country` (`C`) = `NL`.
- Sleutels: **RSA-4096** (spiegelt OpenFSC).
- cfssl `peer`-profiel usages: `signing, key encipherment, server auth, client auth`. `intermediate`-profiel: `digital signature, cert sign, crl sign, signing`.
- Placeholder-OIN's nu (distinct per peer), echte test-OIN's later → markeer `TODO(#722)`. Placeholder-hostnames tot ZAD-routes vast → `TODO(#723)`.
- Branch: `feature/test-pki-722` (op PR #4). Commit-trailer: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- Markdown lint-clean (repo draait markdownlint op `pki/README.md`).

## File Structure

```text
pki/
  config.json                       cfssl signing-profielen (intermediate + peer)        [Task 1]
  ca.json                           root-CA CSR                                          [Task 1]
  intermediate.json                 intermediate-CA CSR                                  [Task 1]
  init-ca.sh                        root -> intermediate bootstrap                       [Task 1]
  peers/<peer>/<endpoint>/csr.json  committe cert-templates (6 stuks)                    [Task 2]
  issue.sh                          per-endpoint cert uit csr.json                       [Task 2]
  gen-crl.sh                        lege CRL via intermediate                            [Task 3]
  fix-permissions.sh                chmod o-rw op key-bestanden                          [Task 4]
  verify.sh                         openssl-acceptatie-asserts                           [Task 4]
  ca/                               GITIGNORED  root/intermediate keys/pems/crl          [Task 4 .gitignore]
  out/<peer>/<endpoint>/            GITIGNORED  key.pem + cert.pem(chain)                [Task 4 .gitignore]
  README.md                         run-instructies + cfssl-install + checklist          [Task 5]
group/group-config.example.yaml     crl-pad → intermediate.crl                           [Task 5]
.env.example                        paden consistent met out/-layout (check)            [Task 5]
```

Peers/endpoints + placeholder-OIN's:

| peer | endpoints | placeholder-OIN |
|---|---|---|
| `magazijn-org` (provider) | `manager`, `inway` | `00000000000000000001` |
| `uitvraag-org` (consumer) | `manager`, `outway` | `00000000000000000002` |
| `directory` | `directory`, `manager` | `00000000000000000003` |

---

### Task 1: cfssl-config + CA-bootstrap (root → intermediate)

**Files:**

- Create: `pki/config.json`, `pki/ca.json`, `pki/intermediate.json`, `pki/init-ca.sh`
- Output (gitignored, niet committen): `pki/ca/root.pem`, `pki/ca/root-key.pem`, `pki/ca/intermediate.pem`, `pki/ca/intermediate-key.pem`

**Interfaces:**

- Produces: `pki/ca/root.pem` (trust-anchor), `pki/ca/intermediate.pem` + `pki/ca/intermediate-key.pem` (issuer voor Task 2), `pki/config.json` (profielen `intermediate` + `peer` voor Task 2/3).

- [ ] **Step 1: Verifieer cfssl aanwezig (prereq)**

Run: `command -v cfssl && command -v cfssljson`
Expected: beide paden. Zo niet, installeer:

```bash
go install github.com/cloudflare/cfssl/cmd/cfssl@latest
go install github.com/cloudflare/cfssl/cmd/cfssljson@latest
# of via mise/asdf; zie pki/README.md (Task 5)
```

- [ ] **Step 2: Schrijf de CA-configs**

`pki/config.json`:

```json
{
  "signing": {
    "default": { "expiry": "26280h" },
    "profiles": {
      "intermediate": {
        "expiry": "131376h",
        "ca_constraint": { "is_ca": true, "max_path_len": 0, "max_path_len_zero": true },
        "usages": ["digital signature", "cert sign", "crl sign", "signing"]
      },
      "peer": {
        "expiry": "26280h",
        "usages": ["signing", "key encipherment", "server auth", "client auth"]
      }
    }
  }
}
```

`pki/ca.json`:

```json
{
  "CN": "MOZa FSC Testnet Root CA",
  "key": { "algo": "rsa", "size": 4096 },
  "names": [{ "O": "MOZa FSC Testnet", "C": "NL" }],
  "ca": { "expiry": "131400h" }
}
```

`pki/intermediate.json`:

```json
{
  "CN": "MOZa FSC Testnet Intermediate CA",
  "key": { "algo": "rsa", "size": 4096 },
  "names": [{ "O": "MOZa FSC Testnet", "C": "NL" }],
  "ca": { "expiry": "131376h" }
}
```

- [ ] **Step 3: Schrijf `pki/init-ca.sh`**

```bash
#!/usr/bin/env bash
# Copyright © MOZa FSC Testnet — Licensed under the EUPL
# Genereert root + intermediate test-CA (#722). NIET voor productie.
set -euo pipefail

BASE_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
CONFIG="${BASE_DIR}/config.json"
CA_DIR="${BASE_DIR}/ca"
mkdir -p "${CA_DIR}"

# Root: self-signed CA
cfssl genkey -initca "${BASE_DIR}/ca.json" | cfssljson -bare "${CA_DIR}/root"

# Intermediate: self-signed, daarna her-tekenen door root met profiel 'intermediate'
cfssl genkey -initca "${BASE_DIR}/intermediate.json" | cfssljson -bare "${CA_DIR}/intermediate"
cfssl sign -config "${CONFIG}" \
  -ca "${CA_DIR}/root.pem" -ca-key "${CA_DIR}/root-key.pem" \
  -profile intermediate "${CA_DIR}/intermediate.csr" \
  | cfssljson -bare "${CA_DIR}/intermediate"

rm -f "${CA_DIR}/root.csr" "${CA_DIR}/intermediate.csr"
echo "OK: root.pem (trust-anchor) + intermediate.pem in ${CA_DIR}"
```

- [ ] **Step 4: Maak script uitvoerbaar + draai het (de "test")**

Run:

```bash
chmod +x pki/init-ca.sh && ./pki/init-ca.sh
openssl verify -CAfile pki/ca/root.pem pki/ca/intermediate.pem
openssl x509 -in pki/ca/intermediate.pem -noout -issuer -subject
```

Expected: `pki/ca/intermediate.pem: OK`; issuer = `...Root CA`, subject = `...Intermediate CA`.

- [ ] **Step 5: Commit (alleen scripts/configs — GEEN ca/-output)**

```bash
git add pki/config.json pki/ca.json pki/intermediate.json pki/init-ca.sh
git status --porcelain pki/ca | grep . && { echo "FOUT: ca/-artefacten gestaged"; exit 1; } || true
git commit -m "feat(pki): test-CA bootstrap root+intermediate via cfssl (#722)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Peer CSR-templates + `issue.sh`

**Files:**

- Create: `pki/peers/magazijn-org/manager/csr.json`, `pki/peers/magazijn-org/inway/csr.json`, `pki/peers/uitvraag-org/manager/csr.json`, `pki/peers/uitvraag-org/outway/csr.json`, `pki/peers/directory/directory/csr.json`, `pki/peers/directory/manager/csr.json`, `pki/issue.sh`
- Output (gitignored): `pki/out/<peer>/<endpoint>/{key.pem,cert.pem}`

**Interfaces:**

- Consumes: `pki/ca/intermediate.pem` + `pki/ca/intermediate-key.pem` + `pki/config.json` (Task 1).
- Produces: `pki/out/<peer>/<endpoint>/cert.pem` (leaf + intermediate aangehecht) en `key.pem` voor Task 4.

- [ ] **Step 1: Schrijf de 6 `csr.json`-templates**

Patroon (vervang `<CN>`, `<OIN>`, `<O>`):

```json
{
  "CN": "<CN>",
  "key": { "algo": "rsa", "size": 4096 },
  "hosts": ["<CN>"],
  "serialnumber": "<OIN>",
  "names": [{ "O": "<O>", "C": "NL" }]
}
```

Concrete waarden (hostnames = placeholder `TODO(#723)`, OIN = placeholder `TODO(#722)`):

| bestand | CN / hosts | serialnumber | O |
|---|---|---|---|
| `pki/peers/magazijn-org/manager/csr.json` | `manager.magazijn-org.fsc-test.local` | `00000000000000000001` | `magazijn-org` |
| `pki/peers/magazijn-org/inway/csr.json` | `inway.magazijn-org.fsc-test.local` | `00000000000000000001` | `magazijn-org` |
| `pki/peers/uitvraag-org/manager/csr.json` | `manager.uitvraag-org.fsc-test.local` | `00000000000000000002` | `uitvraag-org` |
| `pki/peers/uitvraag-org/outway/csr.json` | `outway.uitvraag-org.fsc-test.local` | `00000000000000000002` | `uitvraag-org` |
| `pki/peers/directory/directory/csr.json` | `directory.fsc-test.local` | `00000000000000000003` | `directory` |
| `pki/peers/directory/manager/csr.json` | `manager.directory.fsc-test.local` | `00000000000000000003` | `directory` |

Voorbeeld `pki/peers/magazijn-org/manager/csr.json`:

```json
{
  "CN": "manager.magazijn-org.fsc-test.local",
  "key": { "algo": "rsa", "size": 4096 },
  "hosts": ["manager.magazijn-org.fsc-test.local"],
  "serialnumber": "00000000000000000001",
  "names": [{ "O": "magazijn-org", "C": "NL" }]
}
```

- [ ] **Step 2: Schrijf `pki/issue.sh`**

```bash
#!/usr/bin/env bash
# Copyright © MOZa FSC Testnet — Licensed under the EUPL
# Issued per-endpoint peer-certs uit pki/peers/*/*/csr.json (#722). -f = forceer her-uitgifte.
set -euo pipefail

BASE_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
CONFIG="${BASE_DIR}/config.json"
CA_CERT="${BASE_DIR}/ca/intermediate.pem"
CA_KEY="${BASE_DIR}/ca/intermediate-key.pem"

FORCE=0
[ "${1:-}" = "-f" ] && FORCE=1

find "${BASE_DIR}/peers" -name csr.json -print0 | while IFS= read -r -d '' CSR; do
  REL="$(dirname "${CSR#"${BASE_DIR}/peers/"}")"   # <peer>/<endpoint>
  OUT_DIR="${BASE_DIR}/out/${REL}"

  if [ -f "${OUT_DIR}/cert.pem" ] && [ "${FORCE}" -eq 0 ]; then
    echo "skip ${REL} (geen -f)"
    continue
  fi

  mkdir -p "${OUT_DIR}"
  echo "cert voor ${REL}..."
  cfssl gencert -config "${CONFIG}" -ca "${CA_CERT}" -ca-key "${CA_KEY}" \
    -profile peer "${CSR}" | cfssljson -bare "${OUT_DIR}/cert"

  cat "${CA_CERT}" >> "${OUT_DIR}/cert.pem"          # hecht intermediate aan (keten)
  mv "${OUT_DIR}/cert-key.pem" "${OUT_DIR}/key.pem"
  rm -f "${OUT_DIR}/cert.csr"
done
echo "OK: certs in ${BASE_DIR}/out"
```

- [ ] **Step 3: Draai uitgifte + verifieer (de "test")**

Run:

```bash
chmod +x pki/issue.sh && ./pki/issue.sh -f
openssl verify -CAfile pki/ca/root.pem -untrusted pki/ca/intermediate.pem \
  pki/out/magazijn-org/manager/cert.pem
openssl x509 -in pki/out/magazijn-org/manager/cert.pem -noout -subject \
  -nameopt utf8,sep_multiline
```

Expected: `cert.pem: OK`; subject bevat `serialNumber = 00000000000000000001` en `O = magazijn-org`.

- [ ] **Step 4: Commit (alleen csr.json-templates + issue.sh — GEEN out/)**

```bash
git add pki/peers pki/issue.sh
git status --porcelain pki/out | grep . && { echo "FOUT: out/-artefacten gestaged"; exit 1; } || true
git commit -m "feat(pki): per-peer endpoint-certs via cfssl issue.sh (#722)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Lege CRL via intermediate

**Files:**

- Create: `pki/gen-crl.sh`
- Output (gitignored): `pki/ca/intermediate.crl`

**Interfaces:**

- Consumes: `pki/ca/intermediate.pem` + `pki/ca/intermediate-key.pem` (Task 1).
- Produces: `pki/ca/intermediate.crl` (PEM) voor group-config-bekabeling (Task 5).

- [ ] **Step 1: Schrijf `pki/gen-crl.sh`**

```bash
#!/usr/bin/env bash
# Copyright © MOZa FSC Testnet — Licensed under the EUPL
# Genereert een lege CRL getekend door de intermediate (issuer van peer-certs) (#722).
set -euo pipefail

BASE_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
CA_DIR="${BASE_DIR}/ca"
EMPTY="$(mktemp)"
trap 'rm -f "${EMPTY}"' EXIT

# cfssl gencrl <serials-file> <ca-cert> <ca-key> <expiry-uren> -> base64(DER)
cfssl gencrl "${EMPTY}" "${CA_DIR}/intermediate.pem" "${CA_DIR}/intermediate-key.pem" 43800 \
  | base64 -d \
  | openssl crl -inform DER -out "${CA_DIR}/intermediate.crl"

echo "OK: intermediate.crl in ${CA_DIR}"
```

- [ ] **Step 2: Draai + verifieer (de "test")**

Run:

```bash
chmod +x pki/gen-crl.sh && ./pki/gen-crl.sh
openssl crl -in pki/ca/intermediate.crl -noout -text | head -20
```

Expected: parseert; `Issuer: ...Intermediate CA`; geen revoked entries.

> Noot: `cfssl gencrl` output-vorm kan per cfssl-versie verschillen (rauwe base64-DER vs JSON `{"crl": "..."}`). Faalt de pipe, inspecteer met `cfssl gencrl ... | head -c 80`; bij JSON gebruik `| cfssljson -bare ...` of `jq -r .crl`. Pas de regel aan en her-test.

- [ ] **Step 3: Commit (alleen script — GEEN .crl)**

```bash
git add pki/gen-crl.sh
git commit -m "feat(pki): lege CRL-generatie via intermediate (#722)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Permissies, gitignore-hardening + acceptatie-`verify.sh`

**Files:**

- Create: `pki/fix-permissions.sh`, `pki/verify.sh`
- Modify: `.gitignore` (voeg `pki/ca/` + `pki/out/` toe)

**Interfaces:**

- Consumes: alle output van Task 1-3.
- Produces: `pki/verify.sh` (acceptatie-poort, exit 0 = alle asserts groen).

- [ ] **Step 1: Hard-en `.gitignore`**

Voeg toe na de bestaande `secrets/`-regel (de globs `*.pem *.key *.crt *.crl` dekken de inhoud al; dit dekt de mappen expliciet):

```gitignore
pki/ca/
pki/out/
```

- [ ] **Step 2: Schrijf `pki/fix-permissions.sh`** (spiegelt OpenFSC)

```bash
#!/usr/bin/env sh
# Copyright © MOZa FSC Testnet — Licensed under the EUPL
# Verwijder world read/write van key-bestanden.
BASE_DIR="$( cd "$( dirname "$0" )" >/dev/null 2>&1 && pwd )"
find "${BASE_DIR}" -name "*-key.pem" -type f -exec chmod o-rw {} \;
find "${BASE_DIR}" -name "key.pem"   -type f -exec chmod o-rw {} \;
```

- [ ] **Step 3: Schrijf `pki/verify.sh` (de "test" — acceptatie #722)**

```bash
#!/usr/bin/env bash
# Copyright © MOZa FSC Testnet — Licensed under the EUPL
# Acceptatie-asserts test-PKI (#722). Exit 0 = groen.
set -uo pipefail

BASE_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
fail=0

# 1. Ketengeldigheid per leaf
for cert in "${BASE_DIR}"/out/*/*/cert.pem; do
  if openssl verify -CAfile "${BASE_DIR}/ca/root.pem" \
       -untrusted "${BASE_DIR}/ca/intermediate.pem" "${cert}" >/dev/null 2>&1; then
    echo "OK  keten: ${cert#"${BASE_DIR}/"}"
  else
    echo "FAIL keten: ${cert#"${BASE_DIR}/"}"; fail=1
  fi
done

# 2. serialNumber (OIN) aanwezig in subject
for cert in "${BASE_DIR}"/out/*/*/cert.pem; do
  if openssl x509 -in "${cert}" -noout -subject -nameopt sep_multiline | grep -q 'serialNumber'; then
    echo "OK  OIN:   ${cert#"${BASE_DIR}/"}"
  else
    echo "FAIL OIN ontbreekt: ${cert#"${BASE_DIR}/"}"; fail=1
  fi
done

# 3. CRL parseert
if openssl crl -in "${BASE_DIR}/ca/intermediate.crl" -noout -text >/dev/null 2>&1; then
  echo "OK  crl"
else
  echo "FAIL crl"; fail=1
fi

# 4. Geen secrets gestaged/untracked-toevoegbaar
if git -C "${BASE_DIR}/.." status --porcelain pki | grep -E '\.(pem|key|crt|crl)$'; then
  echo "FAIL: PKI-secrets zichtbaar voor git"; fail=1
else
  echo "OK  geen secrets voor git"
fi

[ "${fail}" -eq 0 ] && echo "== ALLE ASSERTS GROEN ==" || echo "== FAILURES =="
exit "${fail}"
```

- [ ] **Step 4: Draai de volledige keten + verify (de "test")**

Run:

```bash
chmod +x pki/fix-permissions.sh pki/verify.sh
./pki/init-ca.sh && ./pki/issue.sh -f && ./pki/gen-crl.sh && ./pki/fix-permissions.sh
./pki/verify.sh
```

Expected: laatste regel `== ALLE ASSERTS GROEN ==`, exit 0.

- [ ] **Step 5: Commit (scripts + gitignore — GEEN secrets)**

```bash
git add .gitignore pki/fix-permissions.sh pki/verify.sh
git status --porcelain pki | grep -E '\.(pem|key|crt|crl)$' && { echo "FOUT: secrets gestaged"; exit 1; } || true
git commit -m "feat(pki): permissies + acceptatie-verify + gitignore-hardening (#722)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Wiring naar config + README/docs

**Files:**

- Modify: `pki/README.md` (run-instructies, cfssl-install, checklist afvinken)
- Modify: `group/group-config.example.yaml` (crl-pad → `intermediate.crl`)
- Modify: `.env.example` (paden consistent — verifiëren, alleen wijzigen indien nodig)

**Interfaces:**

- Consumes: scripts + output-paden uit Task 1-4.
- Produces: gebruikersdocumentatie + correcte trust-anchor/CRL-bekabeling.

- [ ] **Step 1: Werk `group/group-config.example.yaml` bij**

Wijzig de `crl`-regel zodat hij naar de intermediate-CRL wijst (issuer van de peer-certs):

```yaml
  trust_anchor:
    # Pad naar de test-CA root (via ZAD attachments gemount). Geen PKIoverheid.
    ca_cert: /etc/fsc/ca/root.pem
    crl: /etc/fsc/ca/intermediate.crl   # CRL getekend door de intermediate (issuer peer-certs)
```

- [ ] **Step 2: Herschrijf `pki/README.md`**

````markdown
# PKI — test-CA als trust-anchor

Gesloten testnet → **eigen test-CA**, geen PKIoverheid (zie `docs/ontwerpkeuzes.md`).
Spiegelt OpenFSC's cfssl-tooling (`open-fsc/pki/`). Ontwerp:
`docs/superpowers/specs/2026-06-24-test-pki-design.md`.

> **Niet voor productie.** Sleutels/certs horen **niet** in deze repo: `pki/ca/` en `pki/out/`
> zijn gitignored. Alleen scripts, CA-configs en `csr.json`-templates staan in git.

## Benodigdheden

- `cfssl` + `cfssljson` (CloudFlare PKI-toolkit), `openssl`.

```bash
go install github.com/cloudflare/cfssl/cmd/cfssl@latest
go install github.com/cloudflare/cfssl/cmd/cfssljson@latest
```

## Gebruik

```bash
./pki/init-ca.sh          # 1. root + intermediate test-CA  -> pki/ca/
./pki/issue.sh -f         # 2. per-peer endpoint-certs       -> pki/out/<peer>/<endpoint>/
./pki/gen-crl.sh          # 3. lege CRL                       -> pki/ca/intermediate.crl
./pki/fix-permissions.sh  # 4. world-rw van keys halen
./pki/verify.sh           # 5. acceptatie-asserts (exit 0 = groen)
```

`pki/ca/root.pem` = trust-anchor voor de group rules (`group/group-config.example.yaml`).

## Een peer toevoegen

Maak `pki/peers/<peer>/<endpoint>/csr.json` met `serialnumber` = de OIN (wordt Peer ID),
`names[].O` = peer-naam, `CN`/`hosts` = de endpoint-hostname. Draai `./pki/issue.sh -f`.

## Te leveren (#722)

- [x] Genereer-script test-CA (root + intermediate) — `init-ca.sh`.
- [x] Per-peer leaf-certs via script — `issue.sh`.
- [x] CRL-distributie — `gen-crl.sh` (lege CRL, intermediate als issuer).
- [ ] Secrets via ZAD `attachments` (encrypted, read-only mount) — **geblokkeerd**, wacht op
      ZAD cert-upload-feature. `TODO(#723)`: mount `pki/ca/root.pem`, `pki/ca/intermediate.crl`
      en per-peer `out/<peer>/<endpoint>/{cert,key}.pem`. Nooit in image bakken.

## Placeholders

- **OIN's** in `csr.json` zijn dummy (`...0001`–`...0003`). `TODO(#722)`: echte test-OIN's.
- **Hostnames** zijn placeholder (`*.fsc-test.local`). `TODO(#723)`: echte ZAD inway-SNI /
  manager-adressen; daarna `./pki/issue.sh -f`.
````

- [ ] **Step 3: Verifieer `.env.example`-paden**

Run:

```bash
grep -E 'TLS_(NLX_ROOT_CERT|ORG_CERT|ORG_KEY)' .env.example
```

Expected: paden verwijzen naar een gemounte ca-root + peer cert/key. Zijn ze al consistent (`/etc/fsc/ca/root.pem`, `/etc/fsc/peer/cert.pem`, `/etc/fsc/peer/key.pem`) → niets wijzigen. Anders breng in lijn met de mount-conventie uit `pki/README.md`.

- [ ] **Step 4: Lint + commit**

Run:

```bash
npx --yes markdownlint-cli2 "pki/README.md" || true
```

Expected: geen errors op `pki/README.md` (corrigeer eventuele MD-violations).

```bash
git add pki/README.md group/group-config.example.yaml .env.example
git commit -m "docs(pki): run-instructies, cfssl-install, CRL-wiring + checklist (#722)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review

**Spec coverage:**

- §1 scope (group/external PKI, internal+mount out of scope) → Task 1-4 + README-checklist (Task 5) markeert mount `TODO(#723)`. ✓
- §3 architectuur root→intermediate→leaf → Task 1 + 2. ✓
- §3 cfssl-profielen → Task 1 `config.json`. ✓
- §4 repo-layout → alle tasks; `ca/`+`out/` gitignored Task 4. ✓
- §5 data-flow stappen 1-4 → Task 1-4 scripts; stap 5 (mount) gedocumenteerd Task 5. ✓
- §6 cert-profiel (serialNumber/O/C/EKU/KU/RSA-4096) → Task 1 config + Task 2 csr.json + verify. ✓
- §7 verificatie (openssl chain/subject/crl/gitignore) → Task 4 `verify.sh`. ✓
- §8 wiring (group-config, README, .env) → Task 5. ✓
- §9 risico cfssl-dependency/ZAD-blokkade/placeholders → Task 1 step 1 + README. ✓
- §10 YAGNI (geen renewal/serve/internal/OCSP) → niets ervan in plan. ✓

**Placeholder scan:** Geen TBD/TODO-in-stappen; alle scripts/configs volledig uitgeschreven. De `TODO(#722)`/`TODO(#723)`-markers zijn bewust bedoelde repo-markers (echte OIN's/hostnames/mount), conform Global Constraints — geen plan-gaten.

**Type/naam-consistentie:** Padnamen (`pki/ca/intermediate.pem`, `pki/out/<peer>/<endpoint>/{cert,key}.pem`), profielnamen (`intermediate`, `peer`) en CRL-bestand (`intermediate.crl`) consistent over Task 1→5. `issue.sh` schrijft `key.pem`; `verify.sh`/`fix-permissions.sh`/README verwijzen naar `key.pem`. ✓
